comments:
    allowedFields: ["name", "email", "message"]
    requiredFields: ["name", "message"]

    branch: "master"

    filename: "entry{@timestamp}"

    commitMessage: "Someone commented on something"

    format: "yaml"

    moderation: false

    name: "xrdt.github.io"

    path: "_data/comments/"

    generatedFields:
        date:
            type: date
            options:
                format: "timestamp-seconds"
