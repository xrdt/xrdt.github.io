<!DOCTYPE html>
<html>
  {% include head.html %}

<body>

  {% include header.html %}

<section class="post--content">
  <p class="post--date">{{ page.date | date: "%B %-d %Y" }}</p>

  <h1>{{ page.title }}</h1>
  {{ content }}

</section>

</body>

</html>
